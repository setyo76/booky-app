VITE_API_BASE_URL=https://library-backend-production-b9cf.up.railway.app/api
```

---

**Tambahkan `.env` ke `.gitignore`** — cek apakah sudah ada, kalau belum tambahkan:
```
# .gitignore
.env
.env.local
.env.production